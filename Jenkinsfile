pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo 'Build Completed'
      }
    }

    #stage('Testing') {
      steps {
        echo 'Testing Completed'
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploying Completed'
      }
    }
 
  }
}
